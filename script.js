const cart=[];const q=s=>document.querySelector(s),drawer=q('#cartDrawer'),overlay=q('#overlay'),count=q('#cartCount'),items=q('#cartItems'),subtotal=q('#subtotal');const money=n=>'₦'+n.toLocaleString('en-NG');function render(){count.textContent=cart.length;items.innerHTML=cart.length?cart.map(x=>`<div class="cart-row"><span>${x.name}</span><strong>${money(x.price)}</strong></div>`).join(''):'<p class="empty">Your bag is waiting.</p>';subtotal.textContent=money(cart.reduce((a,x)=>a+x.price,0))}function openCart(){drawer.classList.add('open');overlay.classList.add('show')}function closeCart(){drawer.classList.remove('open');overlay.classList.remove('show')}document.querySelectorAll('.add').forEach(b=>b.onclick=()=>{cart.push({name:b.dataset.product,price:+b.dataset.price});render();openCart()});q('#cartOpen').onclick=openCart;q('#cartClose').onclick=closeCart;overlay.onclick=closeCart;q('.checkout').onclick=()=>alert('Checkout is ready to connect to your preferred payment provider.');q('#newsletter').onsubmit=e=>{e.preventDefault();q('#formNote').textContent='You’re on the list. Welcome to D’AYE.';e.target.reset()};render();